<div>
    &nbsp;
    <nav>
        <ul class="pagination justify-content-center d-flex flex-wrap">
            {{#.}}
                <li class="page-item {{css}}"><a class="page-link" href="?page={{page}}">{{page}}</a></li>
            {{/.}}
        </ul>
    </nav>
</div>